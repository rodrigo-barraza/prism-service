{
  "shared": {
    "noConversation": "No active session.",
    "dynamicToolActivationDisabled": "Dynamic tool switching disabled. Enable in Settings.",
    "invalidToolsArray": "'tools' must be non-empty list of tool name or domain prefix.",
    "searchActionNudgeDisabled": "IMPORTANT: found tool NOT enabled (isEnabled: false). MUST call enable_tools before use. Ready next turn."
  },

  "enable_tools": {
    "noValidTools": "No match found. Check spelling or use search_tools.",
    "alreadyEnabled": "All requested already enabled — use now.",
    "activated": "Enabled {{count}} tool. Ready next turn."
  },

  "disable_tools": {
    "noneInSet": "No requested tool in active set.",
    "disabled": "Disabled {{count}} tool.",
    "protectedSuffix": " {{count}} core tool protected."
  },

  "discover_and_enable_tools": {
    "noQueryOrDomain": "Need 'query' or 'domain'.",
    "noMatches": "No matching tool.",
    "foundAndEnabled": "Found {{matchCount}}, enabled {{enabledCount}}. Ready next turn.",
    "foundAlreadyEnabled": "Found {{matchCount}}, all already enabled — use now."
  },

  "set_timer": {
    "noConversation": "No active session.",
    "noPrompt": "'prompt' required.",
    "invalidDuration": "'durationSeconds' must be {{min}}-{{max}}. Longer/recurring: use create_cron_job."
  },

  "list_timers": {
    "noConversation": "No active session."
  },

  "cancel_timer": {
    "noTimerId": "'timerId' required.",
    "notFound": "No timer {{timerId}}.",
    "success": "Timer {{timerId}} cancelled."
  },

  "enter_worktree": {
    "noConversation": "No session.",
    "alreadyInWorktree": "Already in worktree (branch: {{branch}}). exit_worktree first.",
    "noWorkspace": "No workspace configured.",
    "success": "Entered worktree. File ops redirected to {{path}}. exit_worktree with 'merge' or 'discard' when done."
  },

  "exit_worktree": {
    "notInWorktree": "Not in worktree. enter_worktree first.",
    "mergeFailed": "Merge failed: {{error}}. Worktree preserved at {{path}}. Resolve conflict or exit_worktree 'discard'.",
    "merged": "{{branch}} merged to main. Workspace restored.",
    "discarded": "{{branch}} discarded. Change dropped. Workspace restored."
  },

  "search_conversations": {
    "queryRequired": "Query required.",
    "databaseUnavailable": "Database unavailable.",
    "noEmbeddings": "No embedded session. Embedding starts after 6+ message."
  },

  "summarize_conversation": {
    "summaryRequired": "'summary' required, non-empty string."
  },

  "ask_user": {
    "questionsRequired": "'questions' list required, non-empty.",
    "invalidQuestion": "Each question needs non-empty 'question' string.",
    "duplicateQuestion": "Duplicate: \"{{preview}}\"",
    "duplicateOption": "Duplicate option \"{{label}}\" in \"{{question}}\"",
    "tooManyQuestions": "Max 4 per call.",
    "noConversation": "No session.",
    "timedOut": "No response within 5 min."
  },

  "list_mcp_resources": {
    "noServers": "No MCP server connected."
  },

  "read_mcp_resource": {
    "missingParams": "'server_name' and 'uri' required."
  },

  "authenticate_mcp_server": {
    "missingServerName": "'server_name' required.",
    "noCredentials": "Need at least one: 'token', 'api_key', or 'env'."
  },

  "create_skill": {
    "missingFields": "name and prompt required."
  },

  "execute_skill": {
    "missingSkillId": "skillId required."
  },

  "delete_skill": {
    "missingSkillId": "skillId required."
  },

  "write_todo": {
    "invalidItems": "'items' must be list."
  }
}
